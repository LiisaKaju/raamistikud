import: 
  - recipe/laravel.php

config:
  repository: 'git@github.com:LiisaKaju/raamistikud.git'
  keep_releases: 2

hosts:
  stage:
    hostname: 'ta24kaju.itmajakas.ee'
    http_user: virt137757
    remote_user: virt137757
    deploy_path: '~/domeenid/www.ta24kaju.itmajakas.ee/ta24'


tasks:
  npm:production:
  - run: 'cd {{release_path}} && npm ci'
  - run: 'cd {{release_path}} && npx vite build'


  opcache:clear:
    - run: killall php84-cgi || true

  deploy:
    - 'deploy:prepare'
    - 'deploy:vendors'
    - 'npm:production'
    - 'artisan:storage:link'
    - 'artisan:optimize:clear'
    - 'artisan:optimize'
    - 'deploy:publish'

after:
  deploy:failed: deploy:unlock

before:
  deploy:success: opcache:clear
